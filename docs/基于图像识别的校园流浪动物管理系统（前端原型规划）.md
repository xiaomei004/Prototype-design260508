
### 基于图像识别的校园流浪动物管理系统（前端原型规划）

#### 第一阶段：需求分析与信息架构

**任务1：一句话描述核心价值**

随时随地拍照识别，为校园流浪动物建立专属数字档案，用科技赋能人宠和谐校园。

**任务2：定义典型用户画像**

* **爱心学生小张：** 喜欢在食堂喂猫，但不知道猫咪的名字和饮食禁忌。核心需求：拍照快速识别猫咪身份，查看档案，记录打卡自己喂过这只猫。
* **动保协会管理员李同学：** 负责校园流浪动物管理。核心需求：审核新生动物档案，更新动物的绝育/健康状态，发布猫粮众筹或紧急救援通知。

**任务3：梳理核心功能模块（5个）**

1. **AI 识别与建档：** 拍照/传图 -> AI 匹配 -> 档案跳转 / 新建档案表单。
2. **校园图鉴与地图：** 列表展示所有动物档案；地图展示动物常驻位置热力点。
3. **动态与社区：** 类似朋友圈的喂养打卡、美图分享、紧急救助发布。
4. **认领与救助：** 在线填写领养申请、猫舍维护状态登记。
5. **个人中心：** 我的打卡记录、我建档的动物、通知消息。

**任务4：绘制信息架构图 (站点地图)**

**Plaintext**

```
校园流浪动物系统 (前端 H5/Web 视口)
├── 首页 (发现广场)
│   ├── 数据看板 (总记录数、今日喂养)
│   ├── 紧急通知跑马灯
│   └── 社区打卡动态流 (按时间排序)
├── 随手拍 (核心工具)
│   ├── 相机唤起/图片上传
│   ├── AI 扫描加载动画
│   └── 结果页 (匹配成功展示档案卡 / 匹配失败展示建档表单)
├── 图鉴 (动物名录)
│   ├── 列表模式 (支持按猫/狗、状态筛选)
│   └── 地图模式 (在湖工大校园地图上标记动物气泡)
└── 我的 (个人中心)
    ├── 用户信息
    ├── 我的志愿足迹
    └── 系统设置
```

---

#### 第二阶段：低保真与高保真视觉规范 (UI/UX 设定)

为了让前端页面好看且统一，我们需要定义一套视觉规范，这相当于 Axure 里的“母版和样式”。

* **主色调：** 温暖的橘黄色（#FF9800）—— 象征大橘猫和温暖的爱心。
* **辅助色：** 柔和的浅灰色（#F5F7FA）作为背景色，强调色使用草绿色（#4CAF50）表示健康/已绝育，红色（#F44336）表示受伤/求助。
* **布局规范：** 采用 Mobile-First（移动端优先）的响应式布局，底部固定 4 个 Tab 导航栏。
* **UI 组件库建议：** Element Plus (Vue3) 或 Vant UI (更适合移动端视觉)。

---

#### 第三、四阶段：交互逻辑与数据传递 (纯前端 Mock 实现)

因为没有后端，我们需要在前端模拟数据交互：

1. **全局状态传递：** 使用前端状态管理（或本地 `localStorage`）存储模拟数据。例如，当在“随手拍”中完成“新建档案”后，这只新动物的数据要能实时追加到“图鉴”列表中。
2. **核心动画模拟：** 随手拍页面必须有一个“图片上传 -> 遮罩层出现 -> 扫描线上下移动（持续2秒） -> 出现匹配结果”的完整前端动画，用来模拟 AI 耗时。
3. **Mock 数据库：** 预设 2-3 只成熟的动物数据用于展示。

---

### 💻 核心实操：直接发给代码工具的 Prompt

为了快速完成前端展示，你可以将你的开发工具（Cursor/Codex）设置为 **Vue3 + TailwindCSS** 环境，然后分步骤发送以下指令：

#### 🚀 步骤 1：生成基础框架与底部母版导航

**请将以下提示词复制给 AI：**

> “请使用 Vue3 + TailwindCSS，为我搭建一个移动端 H5 视口的 Web 应用框架。
>
> 1. 创建一个固定在底部的导航栏 (Bottom Navigation)，包含四个选项：'发现'、'随手拍'、'图鉴'、'我的'。使用相关的前端图标（可以使用 FontAwesome 或直接用 Emoji 🍔/📷/🐾/👤 替代）。
> 2. 主色调设定为温馨的橘黄色 (bg-orange-500, text-orange-500)。
> 3. 设置 Vue Router，使得点击底部导航可以切换上方的主内容区域视图。默认显示'发现'页面。”

#### 🚀 步骤 2：注入 Mock 数据与图鉴页面

**请将以下提示词复制给 AI：**

> “现在我们来实现‘图鉴’页面。
>
> 1. 首先在全局定义一个 Mock 数据数组 `animalList`，包含以下数据：
>    [{ id: 1, name: '大橘', species: '猫', location: '北区食堂', status: '已绝育', features: '亲人, 贪吃', image: '[https://placekitten.com/300/300](https://www.google.com/search?q=https://placekitten.com/300/300)' },
>    { id: 2, name: '学霸狗', species: '狗', location: '图书馆门口', status: '健康', features: '安静, 怕生', image: '[https://placedog.net/300/300](https://www.google.com/search?q=https://placedog.net/300/300)' }]
> 2. 在‘图鉴’页面中，使用网格布局 (Grid) 将这些动物数据渲染成卡片。卡片包含动物头像、名字、位置标签和状态徽章（已绝育用绿色背景，未绝育用灰色）。
> 3. 页面顶部添加一个简单的搜索框和‘猫/狗’筛选按钮（只需实现纯前端的数据过滤逻辑即可）。”

#### 🚀 步骤 3：实现核心交互（AI 随手拍）—— 你的亮点

**请将以下提示词复制给 AI：**

> “现在实现最核心的‘随手拍’页面。我们需要模拟 AI 图像识别的流程，纯前端实现。
>
> 1. 页面中心放置一个巨大的‘点击拍照/上传’区域。
> 2. 用户点击后，隐藏上传按钮，显示一张模拟的预览图，并在图片上方覆盖一个带有‘扫描线上下移动’动画的半透明遮罩，文字提示‘AI 正在提取动物特征...’。
> 3. 使用 `setTimeout` 模拟 2 秒的延迟。
> 4. 2秒后，弹出一个模态框 (Modal) 显示识别结果：
>    * 使用 `Math.random()` 模拟概率。
>    * 如果随机数 > 0.5，判定为【老朋友】，显示‘匹配成功！匹配度 92%’，并展示 Mock 数据里‘大橘’的卡片信息，下方附带一个‘去打卡喂食’按钮。
>    * 如果随机数 <= 0.5，判定为【新成员】，显示‘系统提示：发现校园新物种！’，下方渲染一个表单，包含输入框（暂定昵称）、下拉框（种类）、输入框（发现位置），并提供一个‘生成新档案’的提交按钮。”

按照这三步 Prompt 生成代码，你就能得到一个包含了**完整交互逻辑、数据状态切换和视觉效果**的无后端高保真网页原型。你可以直接在浏览器里点开给老师演示“拍照 -> 扫描 -> 识别出大橘 / 新建表单”的完整闭环！
